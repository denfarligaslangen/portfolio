<!DOCTYPE html>
<html>
  <head>
    <title>Simthebl Portfolio</title>
    <meta charset="UTF-8">
  </head>
  <body>
    <ul>
   {% for technique in techs %}
   <li><a href="" title="{% for i in stats[technique] %}Projekt: {{ i.name }} ID: {{ i.id }} {% endfor %}"> {{ technique }} </a></li>
   {% endfor %}
   </ul>
  </body>
</html>
